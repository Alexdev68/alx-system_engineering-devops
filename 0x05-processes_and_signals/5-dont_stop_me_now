#!/usr/bin/env bash
# This script terminates the 4-to_infinity_and_beyond process
pid=$(pidof -s "4-to_infinity_and_beyond")

if [[ -n "$pid" ]]; then
	echo "Terminated"
	kill -19 "$pid"
else
	echo "Not running"
fi
